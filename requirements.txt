# Core Streamlit App
streamlit==1.39.0

# PDF Processing
pypdf==5.0.1
pdf2image==1.17.1
Pillow==10.0.1

# Math + Embeddings
numpy==1.26.4
sentence-transformers==3.1.1
transformers==4.45.2

# PyTorch (auto-select version compatible with cloud runtime)
torch
torchvision

# Vector Store (for retrieval)
faiss-cpu>=1.9.0

# OCR (optional for image-based PDFs)
easyocr==1.7.1

# LLM Access
openai==1.47.0
